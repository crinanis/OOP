git add – добавляет указанные файлы в индекс
git status- выдает статус репрезитория
git commit – фиксирует добавленные в индекс изменения
git clone — создание копии (удаленного) репозитория
git push — вносим изменения в удаленный репозиторий
git fetch — забирает изменения удаленной ветки из репозитория по умолчания, основной ветки;
Команда git init создает новый репозиторий Git.
git config — вызов этой команды с именем конфигурации Git на глобальном и локальном уровнях проекта. 
--local
--global
--system
Команда git pull используется для извлечения и загрузки содержимого из удаленного репозитория 
и немедленного обновления локального репозитория этим содержимым.
Команда git diff позволяет сравнивать два различных коммита
Команда git stash предназначена для того, чтобы поместить текущие изменения, которые 
вы выполнили в файлах, в отдельное хранилище, и вернуть файлы к исходному состоянию. 
git reset — мощная команда, имеющая множество различных флагов и способов работы. С её помощью удаляются 
или отменяются (без удаления) коммиты, восстанавливаются файлы из истории и так далее
git revert — это операция для безопасной отмены изменений, действие которой направлено в будущее. 
Для отката изменений команда не удаляет из истории 
коммиты или родительские элементы, a создает новый коммит с отменой нужных действий.
Rebase — один из двух способов объединить изменения, сделанные в одной ветке, с другой веткой.
беденяет до последнего некомфликтного состояния
git checkout [<branch>].
Базовый
$ cd "C:\Users\thesi\Desktop\frog"
$ git init
$ git config —local user.name "Eugene"
$ git config —local user.email "eugenealimake@gmail.com"
$ git remote add origin https://github.com/EugeneAlimake/oop_lab1_task2.git
$ git status
$ git add .
$ git commit -m 'Initial commit. Updated from local repo'
$ git push —set-upstream origin master
$ git fetch
$ git log
$ git clone https://github.com/EugeneAlimake/oop_lab1_task1
$ git push
$ git branch dev
git branch -D dev 
$ git checkout dev
$ git merge —abort
$ git merge dev
$ git add .
$ git commit -m 'Merge from dev'
$ git push
1.Git — это набор консольных утилит, которые отслеживают и фиксируют изменения в файлах
2. принцип работы: гит хранит полные копии файлов на сервере + при слиянии веток других
разрабочиков почти все дальнейшие изменения происходит локально [снимки файлов]
2. основное отличие - остальные системы хранят список изменений
3. modified, staged, commited (+untracked)
Изменённый означает, что вы изменили файл, но ещё не зафиксировали его в своем локальном репозитории
Индексированный - это изменённый файл, текущую версию которого вы отметили для включения в следующий коммит (для фиксации в своём локальном репозитории)
Зафиксированный означает, что файл уже сохранён в вашем локальном репозитории
4. индексированный - отмеченный для некст коммита файл (add)
5. фиксированный - закомиченный на локалке файл (commit)
7.pull — Объединенная команда, которая выполняет, fetch а затем merge .
Примените изменения, загруженные fetch с помощью merge команды.
9. git status. 
13. git diff показывает разницу с последним коммитом
15git remote-позволяет создавать, просматривать и удалять подключения к другим репозиториям. 
11. клонирование и то что я знаю
17. просмотр удаленных репозиториев - git remote
18.Pull request — предложение изменения кода в чужом репозитории.
19. git log

Команда diff помогает найти различия между состояниями репозитория/файлов. Она полезна для выявления и предупреждения конфликтов слияния.
